{
  "name": "full-cart",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "lerna run test --stream",
    "web-start:dev": "lerna run start:dev --scope=@full-cart/web --stream",
    "server-start:dev": "lerna run start:dev --scope=@full-cart/server --stream",
    "serve:db": "lerna run serve:db --scope=@full-cart/server --stream",
    "new:release": "learn version --stream",
    "format": "prettier --write . --stream",
    "format:web": "lerna run format --scope=@full-cart/web --stream",
    "format:server": "lerna run format --scope=@full-cart/server --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "repository": "https://github.com/salihcodev/stalkme",
  "author": "@salihcodev",
  "license": "",
  "description": ""
}
